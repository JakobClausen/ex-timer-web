import { theme as defaultTheme } from "@chakra-ui/core";

export default {
  ...defaultTheme,
  colors: {
    ...defaultTheme.colors,
    darkBlue: "#2a363b",
  },
  fonts: {
    ...defaultTheme.fonts,
    // add font
  },
};
